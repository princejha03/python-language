 def calculate_marks():
    sub_num = int(input("enter no of subject:"))
    
    total_marks= 0
    
    for i in range(1,sub_num+1):
        sub_marks=float(input(f"Enter the marks of {i} subjects:"))
        
        total_marks += sub_marks
        total_sub = sub_num
        percentage = (total_marks /(total_sub * 100)) * 100
        
    print(f"Total marks obtained:{total_marks}")
    print(f"total percentage:{percentage}")
        
        

calculate_marks()
